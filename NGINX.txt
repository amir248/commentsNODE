server {
    listen 80;
    server_name comments.qucu.ru;

    # Редирект всех HTTP-запросов на HTTPS
    return 301 https://$host$request_uri;
}

server{
        # listen 443 ssl;
        server_name comments.qucu.ru;

        index index.html index.php;

       	# root /var/www/comments;

        # location / {
        #         proxy_pass http://192.168.1.177:3000;
	# 	proxy_set_header Host $host;

        #         add_header Access-Control-Allow-Origin * always;
        #         add_header Access-Control-Allow-Methods 'GET, POST, OPTIONS' always;
        #         add_header Access-Control-Allow-Headers 'Origin, Content-Type, Accept' always;

        #         if ($request_method = 'OPTIONS') {
        #                 add_header 'Access-Control-Allow-Origin' '*' always;
        #                 add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
        #                 add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization' always;
        #                 add_header 'Access-Control-Allow-Credentials' 'true' always;
        #                 add_header 'Access-Control-Max-Age' 86400 always;
        #                 return 204;
        #         }

	# }



        # OPTIONS — preflight
        # location / {
        #         if ($request_method = OPTIONS) {
        #         add_header Access-Control-Allow-Origin "https://qucu.ru" always;
        #         add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
        #         add_header Access-Control-Allow-Headers "Origin, Content-Type, Accept, Authorization" always;
        #         add_header Access-Control-Allow-Credentials "true" always;
        #         add_header Access-Control-Max-Age 86400 always;
        #         return 204;
        #         }

        #         # Прокси для всех других методов
        #         proxy_pass http://192.168.1.177:3000;
        #         proxy_set_header Host $host;
        #         proxy_set_header X-Real-IP $remote_addr;
        #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #         proxy_set_header X-Forwarded-Proto $scheme;

        #         add_header Access-Control-Allow-Origin "https://qucu.ru" always;
        #         add_header Access-Control-Allow-Credentials "true" always;
        #         add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
        #         add_header Access-Control-Allow-Headers "Origin, Content-Type, Accept, Authorization" always;
        # }

        # location /allow-cors {
        #         proxy_pass http://192.168.1.177:3000/allow-cors;
        #         proxy_set_header Host $host;
        # }

        # location /getIp {
        #         proxy_pass http://192.168.1.177:3000/getIp;
        #         proxy_set_header Host $host;
        # }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/comments.qucu.ru/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/comments.qucu.ru/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

        location / {
        proxy_pass http://192.168.1.177:3000;

        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # CORS headers
        # add_header 'Access-Control-Allow-Origin' 'https://qucu.ru' always;
        # add_header 'Access-Control-Allow-Credentials' 'true' always;
        # add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
        # add_header 'Access-Control-Allow-Headers' 'Origin, Content-Type, Accept, Authorization' always;

        # OPTIONS preflight
        if ($request_method = OPTIONS) {
            # add_header 'Access-Control-Allow-Origin' 'https://qucu.ru' always;
            # add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
            #add_header 'Access-Control-Allow-Headers' 'Origin, Content-Type, Accept, Authorization' always;
            #add_header 'Access-Control-Allow-Credentials' 'true' always;
            #add_header 'Access-Control-Max-Age' 86400 always;
            #return 204;
        }
    }
     # Отдельная локация для получения IP
    location /getIp {
        proxy_pass http://192.168.1.177:3000/getIp;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Отдельная локация для allow-cors
    location /allow-cors {
        proxy_pass http://192.168.1.177:3000/allow-cors;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }



        location /nasoberu.json {
		# proxy_pass http://192.168.1.177:3000/public/json/nasoberu.json;
                proxy_set_header Host $host;
		 add_header 'Access-Control-Allow-Origin' 'https://qucu.ru' always;
	}
        location /public/json/nasoberu.json {
                # add_header 'Access-Control-Allow-Origin' 'https://qucu.ru';
		add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
		add_header 'Access-Control-Allow-Headers' 'DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type';


		proxy_pass http://192.168.1.177:3000/public/json/nasoberu.json;
                proxy_set_header Host $host;
		# add_header 'Access-Control-Allow-Origin' '*' always;
	}
}
